app = "english-tracker-api"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 3001
  force_https = true

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[[volume]]
  source = "data"
  destination = "/app/data"

[env]
  VOLUME_DATA = "/app/data"